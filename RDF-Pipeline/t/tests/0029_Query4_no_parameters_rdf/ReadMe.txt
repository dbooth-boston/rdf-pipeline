This tests the same pipeline that is used in 0028, but without yet
having query parameters.  This is useful for doing a demo: first
this test (0029) can be shown without parameters, and then parameters
can be added by showing 0028.

